<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTEULUGAR - Psicólogo</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.1/css/all.min.css" rel="stylesheet">
    <script src="/js/popper.min.js" defer></script>
    <script src="/js/bootstrap.min.js" defer></script>
  </head>
  <body>
    <header>
      <%- include("../partials/components/navbar/index_prof") %>
    </header>
    <main>
      <% if (typeof op !="undefined" && op==5) { %>
        <%- include('../partials/cliente/index', { cliente: dados }); -%>
      <% } else if (typeof op !="undefined" && op==6) { %>
        <%- include('../partials/cliente/create'); -%>
      <% } else if (typeof op !="undefined" && op==7) { %>
        <%- include('../partials/cliente/update', { cliente: dados }); -%>
      <% } else if (typeof op !="undefined" && op==8) { %>
      <% } else %>
      <% if (typeof op !="undefined" && op==9) { %>
        <%- include('../partials/agenda/index', { agenda: dados }); -%>
      <% } else if (typeof op !="undefined" && op==10) { %>
        <%- include('../partials/agenda/create'); -%>
      <% } else if (typeof op !="undefined" && op==11) { %>
        <%- include('../partials/agenda/update', { agenda: dados }); -%>
      <% } else if (typeof op !="undefined" && op==12) { %>
      <% }%>
    </main>
    <footer>
      <%- include("../partials/components/footer/index") %>
    </footer>
  </body>
</html>